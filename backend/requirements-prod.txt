# Dependencias de RUNTIME. Este es el archivo que instala la imagen Docker.
#
# Está separado de requirements.txt a propósito: pytest, ruff y mypy no tienen
# nada que hacer dentro de un contenedor de producción —son ~60 MB de imagen y
# de superficie de ataque que nunca se ejecutan. requirements.txt incluye este
# archivo con `-r`, así que las versiones de runtime se declaran una sola vez.

# --- Núcleo -------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Base de datos ------------------------------------------------------------
SQLAlchemy==2.0.36
asyncpg==0.30.0            # driver async de la aplicación
psycopg2-binary==2.9.10    # driver sync que usa Alembic
GeoAlchemy2==0.16.0        # tipos PostGIS para SQLAlchemy
alembic==1.14.0

# --- Cliente HTTP -------------------------------------------------------------
httpx==0.28.1

# --- Parseo de fuentes sin API ------------------------------------------------
# Los dos collectors de tránsito leen lo que hay, no lo que quisiéramos: un feed
# RSS y una página de WordPress. Ninguna de las dos fuentes publica JSON.
feedparser==6.0.11         # RSS/Atom tolerante a feeds mal formados
beautifulsoup4==4.12.3     # scraping del portal del MTT
lxml==5.3.0                # parser de bs4: más rápido y mucho más indulgente
                           # con HTML roto que el html.parser de la stdlib

# --- Extracción de entidades con LLM ------------------------------------------
# `google-genai`, NO `google-generativeai`. El segundo es el SDK antiguo: está
# archivado, no recibe modelos nuevos y sus módulos de Vertex se eliminaron el
# 24 de junio de 2026. El unificado trae `client.aio`, que es async nativo — ver
# app/collectors/traffic/gemini.py.
google-genai==1.21.0
