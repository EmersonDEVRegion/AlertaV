# Entorno de DESARROLLO: runtime + herramientas.
#
# Las versiones de runtime viven en requirements-prod.txt, que es lo único que
# instala la imagen Docker. Una sola fuente de verdad: si acá se fijara otra
# versión de FastAPI, producción y local dejarían de parecerse y el bug se
# descubriría en el despliegue.
-r requirements-prod.txt

# --- Desarrollo y pruebas -----------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
respx==0.22.0              # mock de httpx para testear collectors sin red
ruff==0.8.6
mypy==1.14.1
